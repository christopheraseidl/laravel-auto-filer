services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - WWWUSER=${UID:-1000}
      - WWWGROUP=${GID:-1000}
    user: "${UID:-1000}:${GID:-1000}"
    volumes:
      - .:/var/www/html
    working_dir: /var/www/html
    tty: true