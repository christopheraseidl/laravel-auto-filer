#!/bin/bash

# Check if at least one argument is provided
if [ $# -eq 0 ]; then
    echo "Usage: dc [command]"
    exit 1
fi

# Pass all arguments to docker-compose exec app
docker-compose exec app "$@"